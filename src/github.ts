import * as core from "@actions/core";



export const createReleaseTag = () => {
  core.startGroup("createReleaseTag");

  core.endGroup()
}

export const deleteRelease = () => {
  core.startGroup("deleteRelease");

  core.endGroup()
}

export const createRelease = () => {
  core.startGroup("createRelease");

  core.endGroup()
}

export const findRelease = () => {
  core.startGroup("findRelease");

  core.endGroup()
}

export const getCommitsSinceRelease = () => {
  core.startGroup("getCommitsSinceRelease");

  core.endGroup()
}

export const createCommitChangelog = () => {
  core.startGroup("createCommitChangelog");

  core.endGroup()
}
